name: Create Release

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Release type'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  bump-version:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - uses: actions/checkout@v4

      - name: Install jq
        run: sudo apt-get install -y jq

      - name: Pull latest changes
        run: git pull origin main

      - name: Bump version if needed
        id: version
        run: |
          RELEASE_TYPE="${{ github.event.inputs.release_type }}"

          # Read current version
          MAJOR=$(jq -r '.major' version_config.json)
          MINOR=$(jq -r '.minor' version_config.json)
          PATCH=$(jq -r '.patch' version_config.json)

          if [ "$RELEASE_TYPE" == "major" ]; then
            NEW_MAJOR=$((MAJOR + 1))
            NEW_MINOR=0
            NEW_PATCH=0
          elif [ "$RELEASE_TYPE" == "minor" ]; then
            NEW_MAJOR=$MAJOR
            NEW_MINOR=$((MINOR + 1))
            NEW_PATCH=0
          else
            # patch - no version change
            NEW_MAJOR=$MAJOR
            NEW_MINOR=$MINOR
            NEW_PATCH=$PATCH
          fi

          # Update version_config.json if version changed
          if [ "$RELEASE_TYPE" != "patch" ]; then
            jq --arg major "$NEW_MAJOR" --arg minor "$NEW_MINOR" --arg patch "$NEW_PATCH" \
              '.major = ($major | tonumber) | .minor = ($minor | tonumber) | .patch = ($patch | tonumber)' \
              version_config.json > version_config.json.tmp
            mv version_config.json.tmp version_config.json
          fi

          # Export version
          VERSION="$NEW_MAJOR.$NEW_MINOR.$NEW_PATCH"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Commit version bump
        if: github.event.inputs.release_type != 'patch'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add version_config.json
          git commit -m "Bump version to ${{ env.VERSION }} [release] [version bump]"
          git pull --rebase origin main
          git push

  build:
    needs: bump-version
    runs-on: ubuntu-latest
    strategy:
      matrix:
        build_env: ['local-library-v1', 'local-library-v2', 'hardware-v3']

    steps:
      - uses: actions/checkout@v4
        with:
          path: ./Celestial-Wayfinder
          ref: main

      # Also checkout esp-utilities repo
      - uses: actions/checkout@v4
        with:
          repository: WyrdWyrks/esp32-utilities
          path: ./esp32-utilities

      - name: Pull latest changes
        working-directory: ./Celestial-Wayfinder
        run: git pull origin main

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PlatformIO Core
        working-directory: ./Celestial-Wayfinder
        run: pip install --upgrade platformio

      - name: Generate Version Header
        working-directory: ./Celestial-Wayfinder
        run: python generate_version.py

      - name: Build PlatformIO Project
        working-directory: ./Celestial-Wayfinder
        run: pio run --environment ${{ matrix.build_env }}

      - name: Rename firmware binaries
        working-directory: ./Celestial-Wayfinder
        run: |
          mkdir -p release-artifacts
          cp .pio/build/${{ matrix.build_env }}/firmware.bin release-artifacts/firmware-${{ matrix.build_env }}-${{ needs.bump-version.outputs.version }}.bin

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.build_env }}-${{ needs.bump-version.outputs.version }}
          path: ./Celestial-Wayfinder/release-artifacts/*.bin

  create-release:
    needs: [bump-version, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Pull latest changes
        run: git pull origin main

      - name: Set version
        run: |
          echo "VERSION=${{ needs.bump-version.outputs.version }}" >> $GITHUB_ENV

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Git Tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "v${{ env.VERSION }}" -m "Release version ${{ env.VERSION }}"
          git push origin "v${{ env.VERSION }}"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.VERSION }}
          name: Release v${{ env.VERSION }}
          body: |
            Release version ${{ env.VERSION }}

            Release type: ${{ github.event.inputs.release_type }}
          files: artifacts/**/*.bin
          draft: false
          prerelease: false
