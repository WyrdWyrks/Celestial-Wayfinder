# CelestialWayfinder GH Pages

This GitHub pages hosts releases firmware files from Actions releases.
Since GitHub pages has no CORS headers this allows the PWA app to
download releases through Javascript fetch.

